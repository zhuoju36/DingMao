# 建工法律顾问 / AGENTS.md

> 项目导航与协作约定。**不放具体代码**，所有细则在 `docs/` 下按需查阅。

---

## 元信息

| 项 | 值 |
|---|---|
| 项目名 | 建工法律顾问 (Lawyer) |
| 项目所有者 | 1 人独立开发 |
| 创建日期 | 2026-05 |
| 适用对象 | 项目所有者 + AI 编码助手（Claude Code、Cursor、ChatGPT、DeepSeek Harness 等） |

---

## 一、核心原则（项目级红线）

### 开发原则

任何代码、决策、文档变更都不得违反这 9 条：

1 当前系统还在早期开发阶段，未上线，不需要考虑向后兼容。
2 优先使用能满足当前需求的最简单实现。不要预防性抽象，不要多此一举的配置层。
3 系统分层生长。先跑通一个最小的端到端版本，再往上加东西。绝不为了未完成的复杂度拆掉能跑的东西。
4 组件保持模块化，关注点分离。
5 优先使用成熟的、有人维护的库。没有明确理由不要自己重写。
6 先翻项目里已有的依赖能做什么，再考虑加新包或者自己写。
7 架构决策要有长远眼光。不接受"先这样以后再换"的临时方案。
8 先看成熟产品怎么解决同一个问题，用已验证的模式，不要从零发明。
9. **不碰工作区外的文件** — 只要代码或者涉及新增、修改或者删除本目录之外的文件，必须停止，向用户确认

### 应用原则

1. **数据确凿优先** — 结论必须挂 🟦 用户事实 + 🟨 法条 + 🟥 强条三者之一或组合，无依据不升格结论
2. **LLM 不参与关键数字生成** — 金额、时间、条款号、版本号必须结构化填充，LLM 仅负责润色
3. **法条 / 强条引用必须有版本号 + 生效日期** — 引用过期条文等同误导
4. **文书输出前必须显示前置免责声明** — 律师复核提示放在显眼位置，而非页脚
5. **1 人开发节奏：先做透，再做广** — 优先砍功能不延周期；MVP 12 周红线
6. **MVP 边界严格遵守** — 推荐性标准、第三方对接、多人协作、模拟对方立场 — 均不做
7. **知识库与代码解耦** — 法条/强条结构化结果必须可重建，不依赖容器运行时状态
8. **部署目标锁定** — 腾讯云轻量 + Docker Compose，不上 K8s、不上微服务、不上 Serverless
   
---

## 二、文档地图

AGENTS.md 是**索引**，不是手册。所有细节下沉到 `docs/` 下对应子文档。

### 2.1 docs/ 子目录结构

| 子目录 | 内容 | 何时查阅 |
|---|---|---|
| `docs/product/` | 产品 PRD、用户场景、决策日志 | 改需求前 |
| `docs/architecture/` | 架构图、数据模型、API 设计 | 改架构前 |
| `docs/domain/` | 领域知识（建工法律、通用规范） | 改业务逻辑前 |
| `docs/coding/` | 代码规范、目录结构示例、错误处理约定 | 写代码前 |
| `docs/ai-collab/` | AI 协作约定、Prompt 模板、上下文管理 | 用 AI 编码前 |
| `docs/ops/` | 本地环境、部署、密钥、备份、监控 | 部署 / 运维前 |

### 2.2 必查子文档（按使用频率排序）

1. 📘 `docs/product/README.md` — 产品定义与 MVP 范围
2. 📗 `docs/product/decision-log.md` — 关键决策日志（防反悔）
3. 📙 `docs/architecture/tech-stack.md` — 技术栈与选型依据
4. 📕 `docs/architecture/data-model.md` — 数据模型
5. 📒 `docs/coding/conventions.md` — 代码风格与目录约定
6. 📓 `docs/coding/error-handling.md` — 错误处理约定
7. 🗂️ `docs/ai-collab/README.md` — AI 协作约定
8. 🗒️ `docs/ai-collab/prompt-templates.md` — Prompt 模板
9. 📚 `docs/domain/knowledge-base.md` — 知识库构建规范
10. 🔧 `docs/ops/local-dev.md` — 本地开发环境
11. 🚀 `docs/ops/deployment.md` — 部署流程
12. 🔐 `docs/ops/secrets.md` — 密钥管理
13. 💾 `docs/ops/backup.md` — 备份与恢复

---

## 三、工作区结构（高频查阅项）

完整目录树与命名规范见 `docs/coding/conventions.md`。

```
lawyer/
├── backend/          # FastAPI 后端（Python 3.11）
├── frontend/         # Vue 3 前端（Vite + TS + Element Plus）
├── knowledge-base/   # 知识库原始数据 + 结构化脚本
├── deploy/           # Docker Compose 一键部署
├── docs/             # 本文件链接的所有文档
└── AGENTS.md         # 你正在读的文件
```

---

## 四、AI 编码助手使用约定

完整内容见 `docs/ai-collab/README.md`。本节是最精简的"使用守则"：

1. **永远先读 AGENTS.md 和相关 docs/ 章节，再动手** — 不要凭印象改架构
2. **不在 AGENTS.md 里堆代码示例** — 所有代码示例放 `docs/coding/` 或 `docs/ai-collab/`
3. **修改 AGENTS.md 是严肃决策** — 必须经过项目所有者确认，禁止 AI 自动改本文件
4. **AI 输出必须可追溯** — 结论、引用、文件改动都要说明依据（文件路径、行号、需求来源）
5. **每个任务结束三段式总结** — 做了什么 / 改了哪些文件 / 下一步建议
6. **诚实优先** — 不确定的事直说"不知道 / 需实测"，禁止编造 API、价格、能力
7. **优先改 docs/ 而非改代码** — 任何架构调整先更新 `docs/architecture/`，再动代码

---

## 五、决策日志（最近 5-10 条）

详细理由见 `docs/product/decision-log.md`。本节只列"是什么"，不展开"为什么"。

| 日期 | 决策 |
|---|---|
| 2026-05 | 部署用腾讯云轻量 + Docker Compose（1 人开发，零运维） |
| 2026-05 | LLM 主力 MiniMax-M3 / 备用 DeepSeek-V4-Flash（1M 上下文 + 高性价比） |
| 2026-05 | 数据库 MVP 阶段自建 PostgreSQL（成本可控，后期可平滑迁 TencentDB） |
| 2026-05 | 前端 Vue 3 + Element Plus（项目所有者技术栈偏好） |
| 2026-05 | 不做第三方软件对接（保持独立产品边界） |
| 2026-05 | MVP 知识库：50 部核心法规 + 38 本通用规范全文强条（其他不入库） |
| 2026-05 | 文书生成用 Jinja2 模板引擎（LLM 仅润色，数字条款不交给 LLM） |
| 2026-05 | MVP 周期 12 周；角色机制单一角色 + 视角差异，不做模拟对方立场 |

---

## 六、变更记录

| 日期 | 变更 | 触发原因 |
|---|---|---|
| 2026-05 | 初版建立 | 项目启动 |

---

## 七、附：什么时候不需要读 AGENTS.md

- 单纯修改字符串、调整文案、调样式 → 直接改即可
- 重命名文件（保留目录结构）→ 直接改即可
- 添加单元测试、注释、文档 → 直接改即可

**其他场景都建议先读 AGENTS.md 或对应 docs/ 子文档。**