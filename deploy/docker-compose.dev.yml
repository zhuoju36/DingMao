# 开发环境 - 仅启动 PostgreSQL + Redis，不启动应用（应用本地跑）
# 用法: docker compose -f deploy/docker-compose.dev.yml up -d

services:
  postgres:
    image: postgres:16-alpine
    container_name: lawyer-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: lawyer
      POSTGRES_PASSWORD: lawyer_dev_password
      POSTGRES_DB: lawyer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lawyer"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: lawyer-redis-dev
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data_dev:/data

volumes:
  postgres_data_dev:
  redis_data_dev: